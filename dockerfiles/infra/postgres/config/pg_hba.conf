# TYPE  DATABASE     USER  ADDRESS       METHOD
local   all          all                 trust
host    all          all   127.0.0.1/32  scram-sha-256
host    all          all   ::1/128       scram-sha-256

# Allow application and administration clients through the published Docker port.
# Restrict this CIDR before exposing PostgreSQL outside a trusted private network.
host    all          all   0.0.0.0/0     scram-sha-256

# Optional streaming replication user created by initdb/10-enterprise-ops.sh.
host    replication  all   0.0.0.0/0     scram-sha-256

